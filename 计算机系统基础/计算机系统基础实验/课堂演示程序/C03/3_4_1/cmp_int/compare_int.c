#include <stdio.h>

void main()
{
     int   x;
     unsigned int  y;
     x = -1;
     y = -1;
     if (x > 0)
        printf("%d positive\n", x);
     if (y > 0)
        printf("%d positive\n", y);
}
